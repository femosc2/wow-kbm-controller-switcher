# Built binary — produced by `go build` locally or by CI (GitHub Actions)
wowswitch.exe

# Test sandbox created during manual verification
_sbtest/

# Go build cache / module download dirs (if ever used locally)
/bin/
